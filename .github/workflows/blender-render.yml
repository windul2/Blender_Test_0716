name: Blender Render MP4 v4

on:
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Install Blender
        run: |
          sudo snap install blender --classic
          blender --version
          ffmpeg -version | head -1 || sudo apt-get install -y ffmpeg

      - name: Generate scene script
        run: |
          cat > scene.py << 'EOF'
          import bpy, math, os
          print(">>> SCRIPT START, cwd =", os.getcwd())

          bpy.ops.wm.read_factory_settings(use_empty=True)
          scene = bpy.context.scene

          # 모델링: 토러스
          bpy.ops.mesh.primitive_torus_add(major_radius=1.2, minor_radius=0.4)
          obj = bpy.context.object
          for poly in obj.data.polygons:
              poly.use_smooth = True

          # 네온 Emission 머티리얼
          mat = bpy.data.materials.new("Neon")
          mat.use_nodes = True
          nodes = mat.node_tree.nodes
          nodes.clear()
          out = nodes.new("ShaderNodeOutputMaterial")
          emis = nodes.new("ShaderNodeEmission")
          emis.inputs[0].default_value = (0.0, 1.0, 0.8, 1)
          emis.inputs[1].default_value = 5.0
          mat.node_tree.links.new(emis.outputs[0], out.inputs[0])
          obj.data.materials.append(mat)

          # 바닥 / 조명 / 카메라
          bpy.ops.mesh.primitive_plane_add(size=20, location=(0, 0, -1.5))
          bpy.ops.object.light_add(type='AREA', location=(3, -3, 4))
          bpy.context.object.data.energy = 400
          bpy.ops.object.camera_add(location=(0, -6, 2),
                                    rotation=(math.radians(75), 0, 0))
          scene.camera = bpy.context.object

          # 애니메이션: 90프레임 = 3초
          scene.frame_start, scene.frame_end = 1, 90
          obj.rotation_euler = (0, 0, 0)
          obj.keyframe_insert("rotation_euler", frame=1)
          obj.rotation_euler = (math.radians(360), 0, math.radians(360))
          obj.keyframe_insert("rotation_euler", frame=90)

          # 렌더 설정
          scene.render.engine = 'CYCLES'
          scene.cycles.device = 'CPU'
          scene.cycles.samples = 16
          scene.cycles.use_denoising = True
          scene.render.resolution_x = 640
          scene.render.resolution_y = 360
          scene.render.fps = 30

          # PNG 프레임 시퀀스로 출력 (Blender 5.x 호환)
          frames_dir = os.path.join(os.environ.get("OUT_DIR", os.getcwd() + "/output"), "frames")
          os.makedirs(frames_dir, exist_ok=True)
          scene.render.image_settings.file_format = 'PNG'
          scene.render.filepath = os.path.join(frames_dir, "frame_")
          print(">>> RENDER START, frames_dir =", frames_dir)

          bpy.ops.render.render(animation=True)
          print(">>> RENDER DONE, frames:", len(os.listdir(frames_dir)))
          EOF

      - name: Render frames
        env:
          OUT_DIR: ${{ github.workspace }}/output
        run: blender -b --python-exit-code 1 -P scene.py

      - name: Encode MP4 with ffmpeg
        run: |
          set -e
          cd output
          ls frames | head -3
          ffmpeg -y -framerate 30 -i frames/frame_%04d.png \
            -c:v libx264 -pix_fmt yuv420p -crf 20 anim.mp4
          ls -al anim.mp4

      - name: Upload MP4
        uses: actions/upload-artifact@v4
        with:
          name: blender-mp4
          path: output/anim.mp4
          if-no-files-found: error
